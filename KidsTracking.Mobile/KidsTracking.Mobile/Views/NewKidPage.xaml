<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="KidsTracking.Mobile.Views.NewKidPage"
             Title="New Kid">
    
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Cancel" Clicked="Cancel_Clicked" />
        <ToolbarItem Text="Save" Clicked="Save_Clicked" />
    </ContentPage.ToolbarItems>
    
    <ContentPage.Content>
        <StackLayout Spacing="20" Padding="15">
            <Label Text="Name" FontSize="Medium" />
            <Entry Text="{Binding Item.Name, Mode=TwoWay}" FontSize="Small" />
            <Label Text="Phone" FontSize="Medium" />
            <Editor Text="{Binding Item.Phone, Mode=TwoWay}" FontSize="Small" Margin="0" />
            <Label Text="Age" FontSize="Mediam"/>
            <Editor Text="{Binding Item.Age, Mode=TwoWay}" FontSize="Small"/>
            
            <Label Text="Email" FontSize="Small" />
            <Editor Text="{Binding Item.Email, Mode=TwoWay}" FontSize="Small"/>
            <Label Text="Password" FontSize="Medium" />
            <Editor Text="{Binding Item.Password, Mode=TwoWay}" FontSize="Small"/>

        </StackLayout>
    </ContentPage.Content>
    
</ContentPage>
